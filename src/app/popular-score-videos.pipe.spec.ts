import { PopularScoreVideosPipe } from './popular-score-videos.pipe';

describe('PopularScoreVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularScoreVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
