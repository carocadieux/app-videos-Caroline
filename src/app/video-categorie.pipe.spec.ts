import { VideoCategoriePipe } from './video-categorie.pipe';

describe('VideoCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
